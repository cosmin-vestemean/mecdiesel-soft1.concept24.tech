# Current Focus

## Last Updated
- 03.09.2026 (sesiunea 15 — Compute read-only pe `RUNID=4` confirmă diagnosticul OD+podea și toate controalele)

## Current Goal
- Rescrierea sparse din Next Step-ul sesiunii 13 este deployată și validată live: `Prepare` și SKU `Classify` nu mai materializează `#Weeks`/`#WeeklySeries`; `SIGMA_WK` și `SIGMA_MTH` folosesc momente cu `n=52/@NrSaptamani` și `n=12`, iar `Classify @Persist=1` scrie `CCCMINMAXWEEK` și `CCCMINMAXWINSOR`.
- Smoke `RUNID=3`, SKU 1360919: 14/14 filiale, 494 rânduri săptămânale rare, 1 rând winsor, rerulare idempotentă; `SIGMA_WK` sparse = oracolul dens la 8 zecimale pe toate filialele; Classify 22s, Compute `DONE` în 0s.
- Rulare completă `RUNID=4`: Classify `DONE` în 53s, Compute `DONE` în 41s; `713.818 = 50.987 × 14`, `HQ_ROWS=50.987`, 338.485 rânduri `CCCMINMAXWEEK`, 50.987 `CCCMINMAXWINSOR`, 0 rânduri audit goale. Controale: `NULL_SIGMA=0`, `NULL_CLASSIFICATION=0`, `FORCED_Z_MISMATCH=0`, `NEGATIVE_AVG=0`, `MIN_GT_MAX=0`, `NEGATIVE_RESULT=0`, `PACK_MISMATCH=0`, `NULL_RESULT=0`.
- Controlul vechi `OD_NONZERO_ROWS` a raportat 7.974 fals pozitive: toate sunt exclusiv branch 2200 și au `PODEA_APLICATA=1`, override intenționat conform E11/E12. Local, summary-ul Compute separă acum `OD_NONZERO_ROWS` fără podea (0 pe RUNID 4) de `OD_FLOOR_OVERRIDE_ROWS` (7.974). Această corecție de diagnostic este sincronizată în AJS, dar încă nedeployată; nu schimbă rezultate persistate și nu cere rerularea Compute.
- Compute read-only pe `RUNID=4` (`@Persist=0, @SummaryOnly=1`) a confirmat: `TOTAL_ROWS=713.818`, `DISTINCT_ITEMS=50.987`, `DISTINCT_BRANCHES=14`, `HQ_ROWS=50.987`, `OD_NONZERO_ROWS=0`, `OD_FLOOR_OVERRIDE_ROWS=7.974`, `MIN_GT_MAX_ROWS=0`, `NEGATIVE_RESULT_ROWS=0`, `PACK_MISMATCH_ROWS=0`, `NULL_RESULT_ROWS=0`. Indicatorii de stare sunt `HQ_CAP_ROWS=2.918`, `PODEA_ROWS=8.559`, `WARN_VZ26_ZERO_ROWS=624.398`, `WARN_STOC_NEG=26`, `WARN_STOC_MORT=129.896`, `DISC_FLAG_ROWS=43.820`; totalurile sunt `ENG_MIN=132.217,6`, `ENG_MAX=228.327,45`, `BUY_QTY=72.670`.
- Utilizatorul a deployat `NewMinMax.js` și a rulat `/JS/NewMinMax/setup`. S1 confirmă `CCCMINMAXWEEK`, `CCCMINMAXWINSOR`, `sp_MinMaxEngine_Classify`, `sp_MinMaxEngine_Compute`, coloanele `SIGMA_WK_SUMSQ`/`SIGMA_MTH_SUMSQ` și diagnosticul `OD_FLOOR_OVERRIDE_ROWS` în definiția Compute.
- Review-ul focalizat este închis fără defecte blocante. Corecțiile post-review păstrează `MAX_LUNA_QTY >= 0` când există luni absente și acumulează `SUMSQ` în `FLOAT`, ca vechiul `STDEV`, fără limita artificială `DECIMAL(19,8)`. Toate cele 7 perechi SQL/AJS sunt sincronizate, sintaxa AJS și diagnostics sunt curate, iar testele algebrice dens/sparse trec.
- Dry-run-ul de producție a găsit scriptul corect `NewMinMax` în baza `mecdiesel`; commit-ul a fost refuzat server-side prin `ALLOW_WRITE=0 in CCC_WSMCP_AUTH`. Utilizatorul preia manual deploy-ul și rulările S1.
- `PLAN_IMPLEMENTARE.md` fully revised with all client confirmations from 14.08.2026 (I5–I14, E1–E15, F1/F5/F10, L3/L4) — plan is now the implementation source of truth.
- Faza 0 delivered and verified live in S1: `new_min_max/sql/00_params.sql` ran clean — 4 tables (`CCCMINMAXPARAMS` 23 rows, `CCCMINMAXCOV` 33, `CCCMINMAXBRANCH` 18, `CCCMINMAXTEMPLATE` 0 + index), all seed values correct.
- Item #1 (4 branches) resolved via DB: ARAD/VOLUNTARI/MIHAILESTI/RM VALCEA have `WHOUSE.ISACTIVE=0` — closed warehouses, not business decisions. `INCLUS=0` is de facto.
- Effort estimate produced: ~200h total (175–215 range), phase breakdown calibrated on `DEVIZ_IUNIE_2026.md` rates.
- Faza 1a deployed and verified live in S1: `/JS/NewMinMax/setup` installed `dbo.ufn_MinMaxSalesLines(@Company)` successfully. On company 1000 / mode `CLIENT`: 237,057 lines, 18 attributed branch codes, 0 NULL branches, 0 excluded-customer/prefix leaks, 0 duplicate source keys; window 08.09.2025–01.09.2026.
- Faza 1b implementată, instalată și verificată complet pe date reale de producție: `/JS/NewMinMax/setup` a creat/actualizat `dbo.sp_MinMaxEngine_Prepare`. Execuția completă pe producție (`@Company=1000, @Mtrl=NULL, @SummaryOnly=1`) a rulat în 180s (~3 min) și a validat 713.370 rânduri (50.955 articole unice x 14 filiale active, 50.955 HQ, 50.955 Podea, 144.709 cu vânzări). Toate verificările de integritate sunt impecabile: 0 NULL_SIGMA, 0 NULL_MIN_DOC, 0 INVALID_WINDOW, 0 INVALID_WEEK, 0 INVALID_LAST_SALE. Monotonia ferestrelor este 100% respectată.
- Faza 2 implementată, instalată și validată live în producție: `/JS/NewMinMax/setup` a creat/actualizat `dbo.sp_MinMaxEngine_Classify`. Smoke test pe MTRL 1360919 (14 filiale) a demonstrat funcționarea completă și corectă a algoritmului:
  - HQ agregat (1000): Clasa `AY`, `COV_TGT=2.50` (AY×MARE), `SL=95%`, `AVG=103.61`, `ad=3.4536`, `CV=0.5734`.
  - București Podea (2200): Clasa `AX` (CV=0.448 <= 0.5), `COV_TGT=2.75` (AX×MARE), `SL=95%`, `AVG=27.27`, `ad=0.909`.
  - Filiale mici (ex. 1500): Clasa `AY`, `MARIME=MIC` -> `COV_TGT=1.10` (diferențiere corectă conform matricei `CCCMINMAXCOV`).
  - Filiale cu variație mare (ex. 2800): CV=1.047 -> Clasa `AZ`, `COV_TGT=2.00`.
  - Toate politicile de stoc, SL, SSF (1.28), LT (30 zile), FRECVENȚĂ (14 zile), calcul ponderat AVG (`0.30*VZ_4S + 0.40*(VZ_13S/3) + 0.15*(VZ_26S/6) + 0.15*(VZ_52S/12)`) și Pareto ABC funcționează impecabil.
- Branch Replenishment fix deployed & verified live: production `sp_GetMtrlsData` updated with `GROUP BY A.mtrl, C.BRANCH, B.BRANCHSEC` in `#PendingOrders` and active warehouse filter from `reumplere/sp_GetMtrlsDat.sql`. Tested and working correctly for `2200 → 1200,1300,1400`.
- Faza 2b implementată, instalată și verificată live în producție: `/JS/NewMinMax/setup` a creat/actualizat `dbo.sp_MinMaxEngine_ClassifyGroup` (`new_min_max/sql/02_classify_group.sql` și `S1-MEC/AJS/NewMinMax.js`). Execuția de test pe date reale (`@Company=1000, @SummaryOnly=1`) a rulat cu succes complet:
  - 588 rânduri rezultate (42 grupe de produs distincte cu vânzări x 14 filiale active).
  - 500 rânduri `STANDARD`, 3 `NOU`, 85 `OD`.
  - Distribuție clase `STANDARD`: AX=117, AY=40, AZ=20, BX=58, BY=56, BZ=23, CX=21, CY=72, CZ=93, NOU=3, OD=85 (total 588).
  - Integritate perfectă: 0 NULL_LIFECYCLE, 0 NULL_ABC, 0 NULL_XYZ, 0 NULL_CLASA, 0 FORCED_Z_MISMATCH, 0 NEGATIVE_VZ, 0 NULL_SIGMA.
  - Același pipeline ca Faza 2 (winsorizare p95 per SKU -> netting -> serie 52S cu zerouri -> 12 bucket-uri lunare), agregat pe `MTRGROUP` x `BRANCH` cu Pareto ABC concurent per filială.
- Pasul 0 din Faza 3 este închis: eligibilitatea HQ nu mai depinde de `WHOUSE` din compania demo, în **toate cele trei** proceduri care construiesc `#ActiveBranches` (`Classify`, `Prepare`, `ClassifyGroup`). Deploy confirmat live pe toate trei (`modify_date = 03.09.2026 16:11:58`, identic; `CLASSIFY_HAS_OLD_RULE = PREPARE_HAS_OLD_RULE = GROUP_HAS_OLD_RULE = 0`). Invarianți post-deploy: `Prepare` → `713.650 = 50.975 × 14` SKU×filiale, `ClassifyGroup` → `588 = 42 × 14` grupe×filiale, toate controalele `NULL_*`/`INVALID_*`/`FORCED_Z_MISMATCH`/`NEGATIVE_VZ` la zero. Distribuția claselor pe `ClassifyGroup` este identică cu rularea dinaintea fix-ului — corecția nu schimbă rezultatul funcțional de azi, doar elimină dependența fragilă de compania demo.
- **Numărul de rânduri nu este criteriu de acceptanță.** Fereastra vine din `MAX(TRNDATE)` pe date vii: în aceeași sesiune populația a crescut 50.968 → 50.969 SKU și 221.165 → 221.168 linii, cu `AZI` neschimbat. Se verifică invariante: `TOTAL_ROWS = DISTINCT_ITEMS × DISTINCT_BRANCHES`, `DISTINCT_BRANCHES = 14`, `HQ_ROWS = DISTINCT_ITEMS`, controale la zero.
- D1-D3 pentru Compute sunt închise read-only pe producție:
  - `STOC_QTY`: `MTRFINDATA.QTY1`, confirmat identic cu soldul `MTRBALSHEET` la 8 zecimale pe 5 SKU.
  - `ORD_FURN`: `MTRLINES`, `SOSOURCE=1251`, `PENDING=1`, `RESTCATEG=1`, document neanulat, cantitate directă `QTY1-QTY1COV-QTY1CANC`, filială din `WHOUSE.CCCBRANCH`. Expresia directă a coincis cu `FNSOGETLINEPEND` pe toate cele 4.535 linii.
  - `LAST_RECEIPT`: `MAX(MTRTRN.TRNDATE)` per companie/SKU pentru `SOSOURCE=1251` și `TPRMS.FLG01=1`; lipsa rămâne `NULL`, inclusiv `DISC_FLAG`.
- `sync-check.cjs` acoperă acum **toate** cele 6 blocuri SQL (adăugate `00c_sales_lines.sql` și `00d_prepare.sql`, care lipseau) și are o gardă care eșuează dacă un `get*Sql()` din AJS nu are pereche înregistrată.
- Stratul de persistență adus în față (înainte de Faza 3), implementat și verificat live în producție:
  - `new_min_max/sql/00b_persist.sql` (nou) — `CCCMINMAXRUN` (antet versionat) + `CCCMINMAXDET` (46 coloane de clasificare, PK clustered `(RUNID, BRANCH, MTRL)`), plus o secțiune de **aliniere idempotentă** (`ALTER` ghidat de `INFORMATION_SCHEMA`) pentru instalări deja existente.
  - `sp_MinMaxEngine_Classify` are acum `@Persist BIT = 0` și `@RunId INT = NULL OUTPUT`. Cu `@Persist=0` comportamentul e identic cu cel validat anterior; cu `@Persist=1` scrie rândurile, marchează antetul `DONE` (durată + nr. rânduri) și returnează doar antetul, nu setul complet. Cu `@RunId` existent re-rulează peste aceeași rulare. Eroare -> antet `STATUS='ERROR'` + `THROW` (gardă `XACT_STATE()`).
  - `setup()` creează acum **toate** tabelele custom (`CCCMINMAXPARAMS/COV/BRANCH/TEMPLATE/RUN/DET`), nu doar procedurile; precondiția manuală „rulează întâi Faza 0" a fost eliminată. Seed-urile rămân non-distructive (23/33/18 rânduri neatinse la re-rulare).
  - Deployed și rulat `/JS/NewMinMax/setup` live în producție cu succes: JSON returnat `{"success":true,"tables":["CCCMINMAXPARAMS","CCCMINMAXCOV","CCCMINMAXBRANCH","CCCMINMAXTEMPLATE","CCCMINMAXRUN","CCCMINMAXDET"],"objects":["dbo.ufn_MinMaxSalesLines","dbo.sp_MinMaxEngine_Prepare","dbo.sp_MinMaxEngine_Classify","dbo.sp_MinMaxEngine_ClassifyGroup"]}`.
  - Smoke tests live pe `@Mtrl=1360919` rulate și validate în S1:
    - `RUNID=1` (via batch DECLARE/EXEC): 14 rânduri inserate în `CCCMINMAXDET`, `STATUS=DONE`, 21s. Valorile persistate coincid exact cu clasificarea teoretică (HQ: AY / CV 0.5734 / COV 2.50 / AVG 103.61 / ad 3.4536; 2200: AX / COV 2.75; 1500: AY×MIC / COV 1.10; 2800: AZ / COV 2.00). Tipurile text aliniate la `varchar(128/10/50)`.
    - `RUNID=2` (execuție directă procedură în DBExplorer): antetul s-a afișat direct în interfață, 14 rânduri, `STATUS=DONE`, 21s; a confirmat versionarea (nu a suprascris RUNID 1).
  - Schema Compute este implementată și deployată prin `/JS/NewMinMax/setup`: `00b_persist.sql` adaugă cele 6 coloane `COMPUTE_*` pe `CCCMINMAXRUN` și cele 37 de coloane Compute pe `CCCMINMAXDET`, iar `VZ26_CAP_SENTINEL=9999` este seed-uit non-distructiv în `00_params.sql`. Oglinda `S1-MEC/AJS/NewMinMax.js` este sincronizată și verificată.
  - `new_min_max/tools/sync-check.cjs` + `sql-to-js.cjs` — SQL-ul embedat în AJS este verificat linie cu linie față de `new_min_max/sql/*.sql`; toate cele 4 blocuri sunt IN SYNC.
- **Faza 3 implementată, deployată și validată prin smoke test** (`new_min_max/sql/03_compute.sql`, 594 linii, oglindit în `getComputeProcedureSql()`, înregistrat în `setup()`, pereche nouă în `sync-check.cjs` — toate cele 7 blocuri IN SYNC):
  - Structura: validare `@RunId` (THROW 50010-50014) → parametri → `#Src` → `#Stock` (D1) → `#PendingSup` (D2) → `#ErpLimits` (D3 + flags + `MTRPACK`) → `#Calc` (§3.2) → HQ CAP + re-clamp → podea → `BUY_QTY` → indicatori → `@Persist` (o singură trecere peste `CCCMINMAXDET`) → `@SummaryOnly`.
  - Coloanele verificate pe producție înainte de scriere (`INFORMATION_SCHEMA`, 53 coloane), inclusiv `MTRTRN.TRNDATE` care era doar afirmat în handoff.
  - `WHOUSE.CCCBRANCH` este `int`, `CCCMINMAXDET.BRANCH` este `smallint` → conversie explicită la sursă, ca să nu apară conversii implicite în fiecare join.
  - Deploy verificat: textul din `OBJECT_DEFINITION` este identic linie cu linie cu sursa locală (singura diferență e rescrierea `CREATE OR ALTER` → `CREATE   ` făcută de SQL Server).
  - **Smoke test `@Mtrl=1360919`, `@RunId=1`, `@Persist=0` — corect pe toate valorile verificabile manual:** HQ `BUF=175.1675`, `CYCLE=259.0208` (COV 2.50), `MAX_RAW=435`, `MAX_INF=566` (×1.30), `CAP6=622`, `VZ26_CAP=594` → `ENG_MAX=566`, `ENG_MIN=176`, `BUY_QTY=383`, `FLAG_RATIO=0.9626` (OK), `TREND_PCT=-0.2694` (TREND_DOWN). `SUM_BR_MAX=410` = exact suma celor 13 `ENG_MAX` de filială. HQ CAP neaplicat corect (566 <= 615). Podea pe 2200: `ENG_MIN=53=CEILING(176*0.30)`, `PODEA_APLICATA=1`. `ENG_MIN <= ENG_MAX` pe toate cele 14 rânduri, fără negative/NULL.

## Active Area
- **Cadență confirmată de client (03.09.2026): rularea este LUNARĂ**, acoperind fiecare filială plus agregatul HQ; se ajustează câțiva parametri la fiecare rulare, apoi rezultatul se îngheață până luna următoare. Consecințe: performanța este irelevantă (~2 min/sesiune), storage-ul pentru ~20 de sesiuni păstrate nu e o problemă, iar tiparul „reclasifici rar / recalculezi zilnic" **nu există** — deci `Compute` nu are nevoie să ruleze peste clasificarea unei sesiuni anterioare (propunerea `StartRun @FromRunId` a fost retrasă). **Riscul real este coerența parametrilor în interiorul unei sesiuni**, pentru că parametrii sunt variabila, nu datele. Mitigarea este de orchestrare, nu de schemă: `runEngine` execută `StartRun → Classify → ClassifyGroup → Compute → FinishRun` într-un singur apel, ca parametrii să nu se poată schimba la mijloc.
- **`sp_MinMaxEngine_Prepare` NU este o fază a pipeline-ului.** Propriul antet o declară „acelaşi pipeline ca `Classify`, oprit înainte de clasificare"; nu are `@Persist`, nu scrie în niciun tabel `CCC*`, nimic nu o consumă, iar `Classify` își reconstruiește singură tot lanțul. Este un oracol de validare din Faza 1b — cei 180s ai ei nu intră în costul unei sesiuni.
- **Model de sesiune imutabil, implementat local, nedeployat (03.09.2026).** `RUNID` devine explicit o **sesiune de calcul**: `sp_MinMaxEngine_StartRun` o deschide (`SESSION_STATUS='OPEN'`, `SCOPE` ∈ `FULL`/`SKU`/`GROUP`), fazele scriu doar în sesiuni `OPEN`, iar `sp_MinMaxEngine_FinishRun` o închide și mută `ESTE_CURENT` — doar pe sesiuni `FULL` complete. **Recalcularea înseamnă sesiune nouă, nu rescriere:** ramurile de re-rulare au fost eliminate din `Classify` și `ClassifyGroup`, iar `grep` confirmă **zero** `DELETE` pe tabele persistate. Citirea unei sesiuni închise rămâne permisă (garda din `Compute` e condiționată de `@Persist = 1`). Decizie de business: **fără scenarii what-if**.
- **Opțiunea A pentru `AZI` (confirmată):** antetul **nu** stochează un `AZI` autoritar; rămâne pe rândurile copil, unde e adevărat. Nu s-a atins `ufn_MinMaxSalesLines`, deci nici `Prepare`. Multisesiune se analizează dacă și când apare cerința.
- **Persistență `ClassifyGroup` implementată local, nedeployată.** `CCCMINMAXGRP` (PK clustered `(RUNID, BRANCH, MTRGROUP)`, 36 coloane) plus cele 7 coloane `GROUP_*` pe `CCCMINMAXRUN`. Validarea a fost mutată în capul procedurii (fail fast) și aliniată la `Compute`: existență, companie, sesiune `OPEN`.
- **Coduri `THROW` alocate:** `50004/50007/50008` Classify sesiune, `50005/50006/50015` ClassifyGroup, `50017` Compute sesiune, `50030-50032` StartRun, `50033-50038` FinishRun. Faza 4 își păstrează `50020-50024` din contract.
- `new_min_max/` / `S1-MEC/AJS/NewMinMax.js` — rescrierea sparse, diagnosticul OD+podea și Fazele 0–3 sunt complete, sincronizate și validate live. Contractul Faza 4 (`applyToErp`) este închis și acceptat, dar **amânat deliberat**: prioritatea este acum Faza 5 (UI), ca beneficiarul să confirme vizual ce se calculează deja înainte de pasul ireversibil de scriere în ERP. Calibrarea `FLAG` necesită confirmarea criteriului de business, nu o schimbare automată de praguri.

## Relevant Files
- `new_min_max/FAZA5_CONTRACT.md` — contractul UI complet: împărțirea AJS vs `execSql`, transportul MCP exact (`SQL`/`PARAMS` + aliasuri), environment-only pentru secrete, token UI → `clientID`, contractul de filtre `getRunResults`, sesiunea curentă `ESTE_CURENT` și filtrul inițial. **Punctul de intrare pentru sesiunea Implement.**
- `new_min_max/FAZA4_CONTRACT.md` — contractul complet `applyToErp`, confirmat 03.09.2026 și **amânat după Faza 5**. Se reia ca atare, nu se rediscută.
- `new_min_max/FAZA3_HANDOFF.md` — **punctul de intrare pentru sesiunea Implement**: corecțiile HQ/company 1001, bug-ul de eligibilitate din Classify, formulele Faza 3, arhitectura procedurii, planul de validare.
- `new_min_max/PLAN_IMPLEMENTARE.md` — implementation plan, revised 14.08.2026; §1.1 (closed warehouses), §1.8 (branch attribution), §1.9 (HQ aggregator), §10 (open items) are current.
- `new_min_max/sql/00_params.sql` — Faza 0 DDL + idempotent seed; **executed in production S1, verified**. Oglindit în `getParamTablesSql()`.
- `new_min_max/sql/00b_persist.sql` — `CCCMINMAXRUN` + `CCCMINMAXDET`, noile `CCCMINMAXWEEK`/`CCCMINMAXWINSOR` și alinierea idempotentă pentru momentele `SIGMA_*_SUMSQ`; ultimele adăugiri sunt doar locale.
- `new_min_max/tools/sync-check.cjs` / `sql-to-js.cjs` — verifică/generează oglinda SQL din `S1-MEC/AJS/NewMinMax.js`. **De rulat după fiecare editare de SQL.**
- `new_min_max/sql/00d_prepare.sql` — `sp_MinMaxEngine_Prepare`; rescriere sparse locală, oglindită în AJS, încă nedeployată.
- `new_min_max/sql/01_classify.sql` — Faza 2, `sp_MinMaxEngine_Classify` (per SKU); rescriere sparse + persistență audit implementate local, încă nedeployate.
- `new_min_max/sql/02_classify_group.sql` — Faza 2b, `sp_MinMaxEngine_ClassifyGroup` (per `MTRGROUP` × branch); instalat și validat în producție.
- `new_min_max/sql/03_compute.sql` — Faza 3, `sp_MinMaxEngine_Compute`; instalat și smoke-testat în producție. Oglindit în `getComputeProcedureSql()`.
- `new_min_max/SUMAR_TEORETIC_CONFIRMARE.md` — client confirmation doc; provenance tags `[S]/[E]/[D]`; most items now ✅.
- `DEVIZ_IUNIE_2026.md` — effort-rate reference for estimates.
- `S1-MEC/AJS/NewMinMax.js` — containerul AJS pentru toate obiectele MIN/MAX; hardlink spre submodul, sincronizat cu sursele SQL locale, ultimele modificări încă nedeployate.

## Confirmed Decisions
- **Performanță / serie rară (03.09.2026):** `#WeeklySeries` din `Prepare` și `Classify` materializează `#Items × filiale × 52` ≈ **37M rânduri**, din care doar **199.411 (0,5%)** sunt observații reale (măsurat pe producție). Decizie: seria nu se mai materializează dens; agregatele se calculează din seria rară, iar `SIGMA_WK`/`SIGMA_MTH` din momentele de ordin 1 și 2 cu `n` constant (52 / 12) — rezultat identic cu `STDEV()`. `PLAN_IMPLEMENTARE.md` §3 pasul 3-4 și §4.1 sunt actualizate.
- **`MTRL.ISACTIVE` nu este un filtru util (03.09.2026, măsurat):** doar **12** din 52.700 articole cu vânzări 52S au `ISACTIVE = 0`.
- **`CCCBLOCKPUR` — semantică confirmată de client (03.09.2026):** `ISNULL(CCCBLOCKPUR,0) <> 0` = **blocat la achiziție**. 21.079 articole (40%), 29,0% din valoarea 52S, dar încă **20,9% din ultimele 4 săptămâni** (2.088 articole vândute) → marfă în lichidare care se vinde activ, deci raportul trebuie să o arate.
- **Impact ABC măsurat (03.09.2026, read-only, fără rulare persistată — `VAL_52S` nu e afectat de winsorizare):** pe 64.516 rânduri active `BRANCH × MTRGROUP × MTRL` (13 filiale fizice), scoaterea `CCCEXSTAT` din Pareto schimbă **3.050 clase (4,7%)** — 521 promovări, 2.529 retrogradări, din care **1.226 pierd `A`**; clasa `A` 18.636 → 17.684 (−5,1%). **Direcția este inversă față de ipoteza inițială:** pragul fiind procent din `GRP_TOTAL_VAL`, scăderea totalului face cumulativul să atingă 80% mai devreme, deci `A` se **restrânge**. Comportamentul actual **supra-stochează** (marfa în lichidare umflă totalul grupei și menține 1.226 de articole în `A` cu SL 95%), nu sub-stochează. Alegere de business, magnitudine modestă, nu blochează nimic.
- **Maparea flagurilor MTRL, confirmată din ecranul „Nomenclator articole" (03.09.2026):** `CCCITEMOUTLET` = În lichidare (2.232), `CCCBLOCKPUR` = Blocat achiziții (21.079), `CCCEXSTAT` = **Exclude statistici** (21.139). `CCCEXSTAT` și `CCCBLOCKPUR` sunt 98% același set; `CCCITEMOUTLET` e practic submulțime. **`CCCEXSTAT` este flagul operant**, nu `CCCBLOCKPUR`: [necesar-achizitii/NecesarAchizitie.js](necesar-achizitii/NecesarAchizitie.js) din producție îl filtrează deja dur (`isnull(m.cccexstat,0) = 0` + `m.isactive = 1`), iar motorul nou îl citește doar informativ. Item 13 în plan §10, parametru `INCLUDE_EXSTAT_IN_STATISTICI` default 0. **Grup de risc: 1.614 articole `CCCEXSTAT` cu min/max întreținut manual în `MTRBRNLIMITS`.**
- **Închide pârghia de performanță, dar nu înlocuiește rescrierea sparse:** `INCLUDE_EXSTAT_IN_STATISTICI = 0` taie universul 52.701 → ~31.562 (−40%), dar seria densă ar rămâne la ~22M rânduri, tot cu ~0,5% densitate.
- **Substrat de audit (03.09.2026):** `CCCMINMAXWEEK` (serie rară, ~350k rânduri/rulare) + `CCCMINMAXWINSOR` (~52,7k) adăugate în plan §4.1. Servesc simultan ca intrare pentru agregate și ca substrat pentru `sp_MinMaxEngine_Explain`. Regulă dură: `Explain` citește **exclusiv** starea persistată a rulării, niciodată `MTRTRN`/`FINDOC`/`MTRL` — altfel `AZI` pe date vii face explicația să nu corespundă rulării.
- **Versionare AJS (03.09.2026):** `S1-MEC/AJS/NewMinMax.js` **nu** este urmărit de repo-ul principal — este hardlink către `external/MEC/SyncItalia/S1/AJS/NewMinMax.js`, urmărit de submodul. Hardlink-ul propagă conținutul, nu și commit-ul: orice modificare de AJS cere commit în submodul **și** commit de pointer în repo-ul principal.
- **Transparență (03.09.2026):** toate formulele și deciziile din `Compute` și fazele conexe se publică într-un **wiki HTML atașat aplicației** (tipar: `public/help/zero-minmax-help.html` în `<iframe>`/modal). Wiki-ul se **generează** dintr-o sursă unică, nu se editează manual; parametrii se injectează live din `CCCMINMAXPARAMS`, nu se hardcodează. Coloanele intermediare din `CCCMINMAXDET` (`SAFETY`/`BUF`/`CYCLE`/`CAP6`/...) devin astfel load-bearing — susțin drill-down-ul „explică acest calcul" pe rând. Detalii: `new_min_max/FAZA3_HANDOFF.md` §9.- CCC* tables live in the S1 DB, created via AJS `setup()` + `X.RUNSQL` (`IF NOT EXISTS ... sysobjects`), NOT via knex migrations (those target the Feathers app DB). Din 03.09.2026, `setup()` creează **toate** tabelele custom, nu doar procedurile.
- DDL-ul fiind `IF NOT EXISTS`, un tabel deja creat nu își schimbă singur schema: modificările de coloane se fac prin secțiunea de aliniere ghidată de `INFORMATION_SCHEMA` din `00b_persist.sql`.
- Rulările sunt versionate în `CCCMINMAXRUN`; `sp_MinMaxEngine_Classify @Persist=1` este contractul prin care fazele următoare primesc datele (nu temp tables partajate).
- Engine branch list source of truth: `WHOUSE.ISACTIVE=1 AND CCCBRANCH IS NOT NULL` (13+HQ), not `BRANCH.ISACTIVE` (18, 4 dead).
- **CORECTAT 03.09.2026 (verificat pe producție):** „HQ" (branch 1000) nu este o locație fizică, ci **stratul de companie**, persistat în `MTRL` (`REMAINLIMMIN`/`REMAINLIMMAX`, `CCCMINAUTOCOMP`/`CCCMAXAUTOCOMP`). Materializarea fizică = **București 2200**. Compania **1001 este `Demo S.R.L.`** (`ISACTIVE=0`, 0 articole) — afirmația anterioară „depozitul real al HQ e pe compania 1001" era falsă. `ESTE_HQ` **rămâne** pe branch 1000; mutarea lui pe 2200 ar elimina din calcul propriile vânzări ale Bucureștiului (26,5% din valoarea națională).
- Stock per warehouse: `MTRBALSHEET` (`FISCPRD`, `PERIOD`, `IMPQTY1-EXPQTY1`); `MTRSTATS`/`MTRWHSTOCK` don't exist.
- All 14.08.2026 client confirmations incorporated (plan §10 "Confirmări încorporate"): winsor→netting order, σ_WK real + plancher 1.3, CZ cycle=0, E11/E12 MAX=MIN at floor, N_PACK replaces pack rules, ERP_MAX=MAX_MANUAL, SSF flat 1.28, MECDI2 not excluded, exclusion by TRDR.CODE.
- New params in seed: `MOD_ATRIBUIRE_FILIALA=CLIENT`, `HQ_DIN_AGREGAT_COMPANIE=1`, `FLAGS_ZERO_LA_APPLY=1`, `SIGMA_MIN=1.3`, `CZ_CYCLE_ZERO=1`, `WINSOR_SUB_PRAG=MEDIANA`, `PRAG_REC_HQ=39`/`PRAG_REC_BR=26`.
- **Definiția warning-urilor din Compute (03.09.2026):** `WARN_STOC_MORT` = `ENG_MAX = 0 AND STOC_QTY > 0` (E9) și `WARN_STOC_NEG` = `STOC_QTY < 0` (E10), conform `PLAN_IMPLEMENTARE.md` §325-326 — nu sunt praguri inventate. `FAZA3_HANDOFF.md` §4.3 le enumeră fără definiție, deci sursa este planul. `WARN_VZ26_ZERO` = 1 când `VZ_26S <= 0`, adică exact când s-a folosit sentinela.
- **Modulo pe DECIMAL nu există în T-SQL (03.09.2026):** verificarea multiplului de pack din `FAZA3_HANDOFF.md` §7.4 este scrisă acolo ca `BUY_QTY % N_PACK <> 0`, dar operatorul `%` acceptă doar tipuri întregi — ar fi eșuat la rulare cu „Operand data type decimal is invalid for modulo operator”. Implementat ca `BUY_QTY - FLOOR(BUY_QTY / N_PACK) * N_PACK <> 0`.

## Open Questions
- **Șabloane de parametri — discuție amânată deliberat (03.09.2026).** Decizie de principiu luată: Excel-ul `parametri_furnizori.xlsx` (menționat în documentele de analiză, absent din repo) **nu este o precondiție** — e doar una din trei căi de populare, alături de introducerea manuală în UI și de salvarea ca șablon a unei combinații care a funcționat. Cheia agreată: **un singur șablon per `(FURNIZOR, BRANCH, PREFIX)`**, deci unicitatea o dă constrângerea, nu o euristică de tip „prefixul cel mai lung". Gol de schemă de rezolvat la implementare: `CCCMINMAXTEMPLATE` are `NUME`/`PREFIX`/`FURNIZOR`/`PARAMSJSON` cu unicitate pe `NUME`, dar **nu are coloană `BRANCH`**. De decis atunci și cum tratăm rândul HQ (branch 1000), care nu e locație fizică — șabloane proprii sau cădere pe `GLOBAL`.
- **`LT_ZILE` și `FRECVENTA_ZILE` sunt singurii parametri care ar trebui să varieze per prefix și sunt citiți doar `SCOPE='GLOBAL'`.** Seed-ul din `00_params.sql` îi marchează el însuși `FALLBACK ... de confirmat cu clientul` (30 și 14). `COV_TGT` (matrice `CLASA × MARIME`), `SL_A/B/C` (per clasă) și `SSF` (flat 1.28, confirmat client) sunt deja corect implementate. Consecință: **calibrarea `FLAG` nu poate fi validată** cât timp `LT` e o valoare presupusă — intră liniar în `LT_STOCK` și sub radical în `SAFETY`. Vestea bună: `CCCMINMAXDET` are deja `LT_ZILE` și `FRECVENTA_ZILE` **per rând**, deci urma de audit există fără tabele noi, iar cablarea e aditivă — motorul rulează identic cât timp `CCCMINMAXTEMPLATE` e gol.
- **`SLTS` — închis, a nu se redeschide.** Comportamentul invers intenției teoretice (clasa A primește `0,0526`, clasa C `0,333`) este lacuna **L3** din `analiza/validare_teoretica_formula.md`, iar `SUMAR_TEORETIC_CONFIRMARE.md` consemnează că **clientul a acceptat-o ca atare pe 14.08.2026**, fără corecție.
- **Vederea implicită a UI-ului trebuie filtrată.** Din rularea măsurată: 682.098 din 713.818 rânduri (95,6%) sunt `FARA_REFERINTA` și 624.398 au `VZ_26S = 0`. Fără un filtru implicit, beneficiarul deschide ecranul peste 700.000 de rânduri majoritar irelevante.
- **Criteriul `FLAG`, măsurat pe RUNID 4 (03.09.2026):** 682.098/713.818 rânduri (95,6%) sunt `FARA_REFERINTA`, deci nu intră în calibrare. Pe populația curată — 17.945 rânduri `STANDARD/NOU`, cu `ERP_MAX`, fără lichidare/blocare/excludere — eticheta strictă `OK` (`0,77–1,30`) acoperă 7.348 = 40,9%, dar banda necritică existentă `0,50–2,00` (`DOWN+OK+UP`) acoperă 15.392 = **85,77%**. Recomandare: păstrează pragurile direcționale și reformulează acceptanța „>80% fără abatere majoră”, nu „>80% etichetă OK”; schimbarea semanticii etichetelor cere confirmare client.
- **D2a (03.09.2026):** cele 25 de linii `ORD_FURN` de pe depozitul 8002 „BONURI VALORICE” (`FPRMS 4500`, factură fără stoc) nu au `CCCBRANCH`. Implementarea curentă le include în totalul de companie (rândul HQ) și le exclude de pe filiale. De decis cu clientul dacă se exclud complet.
- **Pierdere de cerere pe filialele închise (03.09.2026):** 2300/2400/2600/2900 au 7,25 mil RON (5,7% din valoarea 52S) atribuiți în mod `CLIENT`, dar `#IncludedLines` face `INNER JOIN #ActiveBranches` → dispar din **ambele** agregate, inclusiv din cel de companie care conduce achiziția de la furnizori. De decis: reatribuire către filiala care servește azi, sau măcar includere în agregatul de companie.
- Faza 2b: formula exactă de agregare ABC pe grupă rămâne de confirmat cu clientul (§3.3.1, item deschis). Implementarea curentă partiționează cumulativul pe `BRANCH`, cu ordonare secundară deterministă pe `MTRGROUP_CODE`.
- `MOD_ATRIBUIRE_FILIALA` final choice (DOC/AGENT/CLIENT) — default `CLIENT`, changes ~35% of line attribution; decide via comparison run.
- `FLAGS_ZERO_LA_APPLY` (E15) — spec says informational-only; default zeroes at ERP write.
- `COV_MEDIU` values — fallback `MEDIU=MIC` until client fills them (editable in UI).
- `CX=2.00 > BY=2.00` deliberate? — spec values kept.
- σ_LT (lead-time variability, L1) absent from formula; receipt history availability unknown.
- σ_WK for NOU class: full 52-week series vs. from-first-sale (E5) — default full series.
- `LT_ZILE=30` / `FRECVENTA_ZILE=14` are invented fallbacks pending per-prefix mapping.
- `00_params.sql` idempotency not yet re-tested (ran once on fresh tables).
- Cross-mode count comparison for `DOC` / `AGENT` / `CLIENT` is still pending: production parameter changes are blocked while project `S1_WRITE_MODE=off`. The active `CLIENT` mode is fully validated read-only.

## Next Step
- **Setup-ul AJS este deployat și validat live (07.09.2026)** — există `CCCMINMAXGRP`, `CCCMINMAXWEEK`, `CCCMINMAXWINSOR`, coloanele `GROUP_*` / `SESSION_STATUS` / `SCOPE` / `ESTE_CURENT`, plus `sp_MinMaxEngine_StartRun` și `sp_MinMaxEngine_FinishRun`. Verificarea read-only a confirmat 5 proceduri și 5 tabele; `CCCMINMAXRUN` are 29 de coloane.
- **Validare live pe `RUNID=5` (07.09.2026):** `StartRun @Scope='FULL'` → `Classify @Persist=1` (`DONE`, 706.734 rânduri, 60s) → `ClassifyGroup @Persist=1` (`DONE`, 602 rânduri, 28s) → `Compute @Persist=1` (`DONE`, 41s) → `FinishRun @RequireCompute=1,@RequireGroup=1` (`DONE`, 322s total). Invariante read-only: `706.734 = 50.481 × 14`, `HQ_ROWS=50.481`, `MIN_GT_MAX=0`, rezultate negative `0`, `CCCMINMAXWEEK=333.408`, `CCCMINMAXWINSOR=50.481`; antetul are `ERRORMSG` și `COMPUTE_ERRORMSG` nule. Confirmat live: `SESSION_STATUS=DONE`, `ESTE_CURENT=1`, `STATUS=DONE`, `GROUP_STATUS=DONE`, `COMPUTE_STATUS=DONE`. `RUNID=5` este acum snapshot-ul curent și imutabil pentru Faza 5. `NewMinMax.js` expune doar `/JS/NewMinMax/setup`; execuția procedurilor se face manual în S1. De verificat ulterior și respingerile: `Classify` fără `@RunId` → `50004`; orice fază pe sesiunea închisă → `50008`/`50015`/`50017`.
- **Pentru UI:** Faza 5 se leagă acum de `RUNID=5` prin `ESTE_CURENT=1`, `SCOPE='FULL'`, `SESSION_STATUS='DONE'`, `STATUS='DONE'`, `GROUP_STATUS='DONE'` și `COMPUTE_STATUS='DONE'`; `RUNID=4` rămâne legacy și nu se marchează manual.
- **Faza 5 (UI) trece înaintea Fazei 4.** Contract în `new_min_max/FAZA5_CONTRACT.md`; execuția merge la `Implement` (Sonnet). „Sesiunea curentă" se rezolvă prin `ESTE_CURENT` + statusul fazei cerute, **niciodată** prin `MAX` per rând ca la Top ABC (`#LatestAbcData` din `reumplere/sp_GetMtrlsDat.sql` compune un colaj din rulări diferite — tipar de evitat).
- Faza 4 `applyToErp`: contract închis în `new_min_max/FAZA4_CONTRACT.md`. **De actualizat la reluare:** `CCCMINMAXAPPLYRUN` propus acolo devine redundant — sub modelul de sesiune, apply este încă o fază cu coloane `APPLY_*` pe antet.
- Confirmare client: acceptanța `FLAG` înseamnă recomandat >80% în banda necritică `0,50–2,00`; nu lărgi eticheta `OK` fără decizie explicită.
